# CLAUDE.md

> Claude / Claude Code 等 AI agent 进项目先读这个。

## 先读

1. `./AGENTS.md` ← 主入口，所有规则和命令在这里
2. 当前所在子目录的 `AGENTS.md`（如果有）

## 三句话总结

1. 这是一个 Electron + React + Go 桌面应用原型
2. 改 TS 跑 `npm run lint:changed`，改 Go 跑 `go test ./...`，提交前跑 `npm run check`
3. 不主动 commit，不主动 broad refactor，不写 `Co-Authored-By`
